// Command-Center status contract (v1). Its schemaVersion is an independent
// counter, unrelated to the core router's schemaVersion "2.0" — the two must
// never be compared or kept in sync.
export const CC_STATUS_SCHEMA_VERSION = "1.0";
export const CC_STATUS_TIMEOUT_MS = 3_500;
